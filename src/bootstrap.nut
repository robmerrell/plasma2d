/**
 * puts
 * Print a string and append a newline character
 * params: string message
 * returns: none
 */
function puts(str) print(str + "\n")

// instatiate the director
//director <- Director()

// run the main file
// require("main")